# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/
.npm-cache/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts
packages/shared/dist/

# Native
.kotlin/
*.orig.*
*.jks
*.keystore
keystore.properties
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

# OPC UA client PKI (auto-generated certificates)
apps/api/pki/
pki/

# Informes CSV generados (archivos de exportación efímeros; no historial).
# OJO: anclar la ruta — un `reports/` suelto también ignoraría src/modules/reports (el código).
apps/api/reports/
/reports/

# generated native folders (expo prebuild) — se regeneran; nunca al repo (incluyen el keystore)
/ios
/android
apps/mobile/ios/
apps/mobile/android/

# Subagent-driven-development scratch state
.superpowers/
